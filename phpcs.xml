<?xml version="1.0"?>
<ruleset name="SierraTecnologia Integrations">
    <description>Coding standard for SierraTecnologia Integrations package</description>

    <!-- Mostrar progresso -->
    <arg value="p"/>

    <!-- Usar cores na saída -->
    <arg name="colors"/>

    <!-- Caminhos a serem verificados -->
    <file>src</file>
    <file>tests</file>

    <!-- Padrão PSR-12 -->
    <rule ref="PSR12"/>

    <!-- Excluir diretórios -->
    <exclude-pattern>*/database/*</exclude-pattern>
    <exclude-pattern>*/resources/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>

    <!-- Configurações adicionais -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.CodeAnalysis.UnusedFunctionParameter"/>
    <rule ref="Generic.Commenting.Todo"/>
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
    </rule>

    <!-- Permitir nomes de métodos sublinhados para testes -->
    <rule ref="PSR1.Methods.CamelCapsMethodName.NotCamelCaps">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
</ruleset>
